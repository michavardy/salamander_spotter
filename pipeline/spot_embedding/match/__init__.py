"""match — placeholder for Phase 4/5 (gallery, retrieval, verify, confidence, identify).

Empty in Phase 0. Planned modules: ``gallery`` (DuckDB vector store), ``retrieve`` (top-k +
coarse prefilter), ``verify`` (RANSAC geometric re-rank), ``confidence`` (quality +
TTA-variance + NN-margin → calibrated confidence + abstain), ``identify`` (photo → match /
new / abstain + confidence). See docs/spot_embedding_aggregation_plan.md §Phase 4-5.
"""
